apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: code
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: "standard"
  volumeName: ps-vol
  resources:
    requests:
      storage: 10Gi